import { TplVarDirective } from './tpl-var.directive';

describe('TplVarDirective', () => {
  it('should create an instance', () => {
    const directive = new TplVarDirective();
    expect(directive).toBeTruthy();
  });
});
