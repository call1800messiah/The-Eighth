import { EstimatedAgePipe } from './estimated-age.pipe';

describe('EstimatedAgePipe', () => {
  it('create an instance', () => {
    const pipe = new EstimatedAgePipe();
    expect(pipe).toBeTruthy();
  });
});
